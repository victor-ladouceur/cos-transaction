package org.isidis.amd.resources.common;


public class NoviceTest {
	//private Novice instance;
	/*
	@Before
	public void before() {
		instance = new Novice(Unit.NAME_DEFAULT);
	}

	@Test
	public void testNovice() {
		assertTrue(instance != null && instance.getName().equals(Unit.NAME_DEFAULT));
	}
	*/
}
