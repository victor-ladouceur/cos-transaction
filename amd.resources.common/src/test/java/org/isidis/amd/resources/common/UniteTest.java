package org.isidis.amd.resources.common;

import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertTrue;

import org.junit.Before;
import org.junit.Test;

public class UniteTest {
	
	public static final String NAME_TEST = "test";
	/*
	private class UniteChild extends Unit {
		public UniteChild(String pName) {
			super(pName);
		}
	};
	
	private UniteChild instance;
	
	@Before
	public void before() {
		instance = new UniteChild(NAME_TEST);
	}

	@Test
	public void testUnite() {
		assertNotNull(instance);
	}
	
	@Test
	public void testUniteType() {
		System.out.println(instance);
		assertTrue(true);
	}

	@Test
	public void testUniteString() {
		assertTrue(instance.getName().equals(NAME_TEST));
	}

	@Test
	public void testSetNGetName() {
		String testName = NAME_TEST.toUpperCase();
		instance.setName(testName);
		assertTrue(instance.getName().equals(testName));
	}
	*/
}
